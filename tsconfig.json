{
  "compilerOptions": {
    "target": "ES2022",
    "module": "ESNext",
    "moduleResolution": "bundler",
    "lib": ["ES2022", "DOM"],
    "strict": true,
    "noEmit": true,
    "esModuleInterop": true,
    "skipLibCheck": true,
    "types": ["vitest/globals", "vite/client"]
  },
  "//": "typecheck covers the self-contained, runnable core (fiscal + normalization + tests). The *.example.* files are illustrative extracts, read for the pattern — not compiled here.",
  "include": ["src/utils", "src/__tests__"]
}
